# 运行环境：dev / stage / prod 等
ENV=dev

# ---------------------------
# PostgreSQL 数据库连接配置
# ---------------------------
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=flower_market_ai     # 这里改成你真实的库名
DB_USER=postgres             # 改成你的用户名
DB_PASSWORD=change_me        # 改成你的密码

# ---------------------------
# 样本表 & 原始表配置
# ---------------------------
# 目标样本表（我们清洗/建模都基于这张表）
FM_TARGET_TABLE=fm_market_price

# 需要写入样本表的原始表列表（逗号分隔）
# 例如：总表 + 某个月表；以后加新表，只需要在这里追加即可
FM_SOURCE_TABLES=market_price,market_price_25_12
